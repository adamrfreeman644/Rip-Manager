{
  "version": "0.20.5",
  "notes": [
    "Keep the 0.20.4 metadata lookup improvements, including prominent media type, release date and Google verification.",
    "Set the mobile browser header, tab theme and bottom browser chrome to black for Rip Manager.",
    "Make Manager update validation more resilient by avoiding a forced base-image pull on every app update.",
    "Increase the Manager health-check wait window and add clearer validation diagnostics for future update failures.",
    "Preserve automatic rollback to the previous working Manager code if validation still fails."
  ]
}
