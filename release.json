{
  "version": "0.19.1",
  "notes": [
    "Make each real Node entry on System → Updates open a detailed Node Updates page.",
    "Show installed and available versions for the Node API, required Ubuntu packages and Node Python packages.",
    "Add separate manual controls for checking dependencies, installing dependency updates and installing the Node API update.",
    "Use request-only SSH credentials and block dependency installation while any drive on that node is active.",
    "Do not perform automatic dependency updates or Ubuntu release upgrades; all update actions remain manual-only."
  ]
}
