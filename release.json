{
  "version": "0.20.6",
  "notes": [
    "Fix the Rip Manager updater self-update bind mount so the Manager rebuild always uses the real host project path instead of the updater container overlay.",
    "Preserve automatic rollback and health validation while allowing the updater sidecar to survive Manager code replacement correctly.",
    "Keep the improved metadata lookup UI with prominent media type, release date and Google verification.",
    "Set the mobile browser theme colour to black directly in the document head as well as at runtime for more reliable black header and bottom browser chrome.",
    "Preserve existing settings, PIN, nodes, history and Rip Node version 0.2.9."
  ]
}
