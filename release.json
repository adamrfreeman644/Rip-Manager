{
  "version": "0.19.2",
  "notes": [
    "Move Node API and dependency version checks and manual update controls onto each real Node’s Manage Node page.",
    "Make each real Node tile on System → Updates a full clickable link to that Node’s Manage Node page.",
    "Make the Simulator tile on System → Updates a full clickable link to its Simulator management page.",
    "Remove the separate Node Updates details page while preserving request-only SSH credentials and active-drive safety checks.",
    "Keep Manager, Node API and dependency installation manual-only."
  ]
}
