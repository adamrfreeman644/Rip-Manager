{
  "version": "0.20.1",
  "notes": [
    "Prevent the Unraid host updater from creating directories under /mnt/user before the user-share filesystem is mounted.",
    "Wait for /mnt/user to become a genuine mountpoint before creating Updater logs or state and before starting the GitHub watcher.",
    "Fail safely after a timeout instead of polluting the RAM root filesystem during early boot.",
    "Preserve existing Rip Manager, node, storage, history and manual update behaviour."
  ]
}
