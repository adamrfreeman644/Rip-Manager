{
  "version": "0.22.11",
  "notes": [
    "Record each program-initiated file change in the affected disc-info.json without logging normal access.",
    "Include reliable file metadata where available: size, timestamps, format/container, duration, bit rate, streams, codecs, dimensions, audio details and embedded tags.",
    "Log media preparation, verified transfers, source cleanup and archive sidecar/image writes with the affected file paths."
  ]
}
