{
  "version": "0.22.29",
  "notes": [
    "Fixes Library Lookup in browsers that block requests to /api/library.",
    "The collection endpoint now uses /api/collection, allowing the Library title list and search results to load normally."
  ]
}
