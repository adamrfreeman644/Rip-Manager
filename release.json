{
  "version": "0.22.28",
  "notes": [
    "Library Lookup now automatically rebuilds its manifest index when it is empty.",
    "Existing media is discoverable immediately after an upgrade without first using the Check manifests tool."
  ]
}
