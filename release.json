{
  "version": "0.22.27",
  "notes": [
    "Includes the complete Library Lookup loading fix.",
    "Library now waits until PIN authentication completes before it requests collection data, preventing a blank page after direct navigation to /library."
  ]
}
