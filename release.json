{
  "version": "0.20.13",
  "notes": [
    "Restore Manager-stored disc titles, year, season, disc number and other media metadata on active ripping tiles when the node live job ID changes or does not match the Manager history record.",
    "Only use the fallback metadata match when both records are active and their start times are compatible, preventing an old completed disc title being carried onto a genuinely new rip.",
    "Force the tile grid to repaint after the fix loads so already-running rips can regain their titles without waiting for a new job.",
    "Keep the 0.20.12 black Android/PWA theme setup, barcode lookup improvements and permanent dedicated updater fix.",
    "Preserve existing data, config, PIN, node settings, storage and history, with Rip Node remaining at v0.2.9."
  ]
}
