{
  "version": "0.20.2",
  "notes": [
    "Move local Rip Manager container updates to the AD53 Shared App Updater.",
    "Keep remote Rip Node update delivery separate because it updates other machines rather than the local Docker stack.",
    "Add a root VERSION marker for the shared updater and validate the running Manager through /api/info.",
    "Use the existing private-repository GitHub token through the shared updater without exposing Docker control to Rip Manager.",
    "Preserve existing Rip Manager data, node configuration, storage and history."
  ]
}
