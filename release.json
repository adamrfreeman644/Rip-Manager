{
  "version": "0.22.12",
  "notes": [
    "Keep a complete current inventory and directory structure in every disc-info.json for existing-library scans and program-managed rip folders.",
    "Capture all reachable media/content files with relative paths and available format, duration, stream, codec and tag metadata.",
    "Retain a separate change-only history for writes, moves, preparation and source cleanup; normal reads remain unlogged."
  ]
}
