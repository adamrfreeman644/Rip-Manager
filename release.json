{
  "version": "0.20.7",
  "notes": [
    "Fix the Manager updater so it no longer deletes or replaces its own running updater script and compose file before the new Manager has been validated.",
    "Stage updater.sh and docker-compose.yml and only swap them into place after the new Manager passes health and version validation.",
    "Keep automatic rollback, code-only backups and existing settings, PIN, nodes and history intact.",
    "Keep the metadata lookup improvements and Google verification from 0.20.4 onward.",
    "Keep the black browser theme metadata for the mobile header, tab colour and bottom browser chrome."
  ]
}
