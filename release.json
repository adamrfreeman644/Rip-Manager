{
  "version": "0.22.9",
  "notes": [
    "Create or safely refresh JSON-only disc-info.json manifests when Verify storage & build library scans existing media.",
    "Process existing imported records as well as newly discovered folders, with clear manifest result counts on every rerun.",
    "Preserve richer valid JSON manifest values, record reliable folder/file metadata, and leave unavailable historical rip fields null.",
    "Support configured media destinations such as 1 ~ Movies, 2 ~ TV, 3 ~ Music, and 4 ~ Audiobooks."
  ]
}
