{
  "version": "0.20.10",
  "notes": [
    "Add improved barcode metadata results with a prominent media-type badge and release date so matches are easier to verify before ripping.",
    "Add a Google this action beside each lookup result for quick independent verification without changing the selected metadata.",
    "Set the browser theme metadata to black so supported mobile browser header, tab and bottom chrome match Rip Manager's dark interface.",
    "Permanently fix the dedicated updater so it removes the existing rip-manager container after building the replacement image, avoiding the container-name conflict that blocked v0.20.9 installs.",
    "Preserve existing data, config, PIN, node settings, storage and history, with Rip Node remaining at v0.2.9."
  ]
}
