{
  "version": "0.22.20",
  "notes": [
    "Enrich disc-info.json by inferring reliable metadata such as year and clean title from conventional folder names like Title (YYYY), while never inventing uncertain facts.",
    "Bundle ffprobe in the Rip Manager image so existing audio and video files actually receive duration, container, bitrate, codec, stream, resolution, audio-channel and embedded-tag metadata.",
    "Add manifest-level media summaries including total duration, human-readable duration, audio/video file counts, codecs and video resolutions.",
    "Upgrade manifest schema to v2 and make rescans refresh existing manifests so the existing library can be backfilled."
  ]
}
