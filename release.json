{
  "version": "0.22.25",
  "notes": [
    "Fixes the Library page/API route collision that caused the title list and search results to remain blank even when SQLite contained indexed manifests.",
    "Library data endpoints now use /api/library while /library remains the browser page.",
    "Corrects UPC recognition so 6–14 digit scans use the smart UPC lookup path."
  ]
}
