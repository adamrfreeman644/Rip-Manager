{
  "version": "0.22.26",
  "notes": [
    "Fixes Library Lookup after opening /library while the controller is locked.",
    "The Library page now loads after PIN authentication has completed, preventing its initial data request from failing and leaving the title list blank."
  ]
}
